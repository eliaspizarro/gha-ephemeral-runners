# ==============================================================================
# CONFIGURACIÓN DE ENTORNO - GHA EPHEMERAL RUNNERS (EJEMPLO)
# ==============================================================================
# Este archivo es usado directamente por docker-compose.yml.
# Copia este archivo a .env y configura los valores según tus necesidades.
# ==============================================================================

## GitHub Token (obligatorio)
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ==============================================================================
# VARIABLES ACTIVAS (sin # al inicio)
# ==============================================================================

## Configuración de Puertos
# API_GATEWAY_HOST_PORT=8080    # Opcional - Puerto host para API Gateway (default: 8080)
API_GATEWAY_PORT=8080            # Puerto interno del contenedor API Gateway
ORCHESTRATOR_PORT=8000           # Puerto interno del contenedor Orchestrator

## Configuración de Registry (para imágenes)
REGISTRY=your-registry.com
IMAGE_VERSION=latest

# ==============================================================================
# VARIABLES OPCIONALES (comentadas con #)
# ==============================================================================
# Descomentar solo si necesitas cambiar el valor por defecto

## API Gateway
# API_KEY=              # Opcional - Clave para autenticación
# ENABLE_AUTH=false     # Opcional - Habilitar autenticación (default: false)
# MAX_REQUESTS=1000     # Opcional - Límite de rate limiting (default: 100)
# RATE_WINDOW=60        # Opcional - Ventana de rate limiting (default: 60)

## CORS Origins (descomentar según entorno)
## Para producción con Nginx Proxy Manager usa https://yourdomain.com
## Para desarrollo local usa http://localhost:3000,http://localhost:8080
# CORS_ORIGINS=https://yourdomain.com


