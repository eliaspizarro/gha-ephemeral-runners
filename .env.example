# ==============================================================================
# CONFIGURACIÓN DE ENTORNO - GHA EPHEMERAL RUNNERS (EJEMPLO)
# ==============================================================================
# Este archivo es una plantilla para el .env usado por docker-compose.yml.
# Copia este archivo a .env y configura los valores según tus necesidades.
#
# NOTA: Las variables con valores por defecto pueden omitirse si se usa el valor
# predeterminado. Las variables obligatorias deben estar configuradas.
# ==============================================================================

# GitHub Token (obligatorio)
# Token con permisos para generar registration tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Configuración del API Gateway
PORT=8080
ORCHESTRATOR_URL=http://orchestrator:8000

# Autenticación (opcional)
API_KEY=TU_API_SECRETA
ENABLE_AUTH=false

# Rate Limiting
MAX_REQUESTS=100
RATE_WINDOW=60

# Configuración del Orquestador
RUNNER_IMAGE=${REGISTRY}/gha-runner:${IMAGE_VERSION}

# Configuración de Registry (solo para construir imágenes)
REGISTRY=your-registry.com

# Version de las imágenes (para producción)
IMAGE_VERSION=latest

# Configuración de Runners
IDLE_TIMEOUT=3600
